import { EVENT_TYPES, COLORS } from "../constants.js";

const tileClickHandler=(index, grids)=>{
    console.log(index, grids);
    // return grids[index]=COLORS.CL04
}

export default tileClickHandler;